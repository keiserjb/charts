# Charts Blocks

Create configurable chart blocks without using Views.

Enable at least one Charts provider submodule, then add the "Charts block" block
through Layouts. The block configuration form provides the shared Charts display
settings and a manual data entry section for labels and series values.

For chart data created by Views, use a Views block display instead.
